version: 1.0.{build}

image: Visual Studio 2019

build_script:
  - npm install
  - cd backend && npm install
  - cd .. && npm run build
  - cd backend && npm run watch

artifacts:
  - path: build
    name: my-app

deploy:
  - provider: GitHub
    artifact: my-app
    draft: true
