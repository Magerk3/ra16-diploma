version: 1.0.{build}

image: node:alpine

build_script:
  - npm install
  - cd backend && npm install
  - cd .. && npm run build

artifacts:
  - path: build
    name: my-app

deploy:
  - provider: GitHub
    artifact: my-app
    draft: true
