export const MenuSearchExpander = ({ handleClick }) => {
    return (
        <div
            data-id="search-expander"
            className="header-controls-pic header-controls-search"
            onClick={handleClick}
        ></div>
    );
};
